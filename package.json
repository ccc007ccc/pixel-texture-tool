{
  "name": "pixel-texture-tool",
  "version": "0.1.0",
  "description": "A local web tool for pixel texture downsampling with per-cell sampling controls.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pixel-art",
    "texture",
    "image-processing",
    "vue",
    "vite"
  ],
  "homepage": ".",
  "scripts": {
    "dev": "vite",
    "typecheck": "vue-tsc --noEmit",
    "build": "npm run typecheck && vite build",
    "check": "npm run typecheck && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vue-tsc": "^2.2.0"
  }
}
